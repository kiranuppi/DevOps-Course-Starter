language: python
services:
- docker
jobs:
  include:
  - stage: build and test
    script:
    - docker build --target test  --tag todo-app:test .
    - docker run -e SECRET_KEY -e API_KEY -e API_TOKEN todo-app:test tests_e2e
    - docker run --env-file ./.env.test todo-app:test tests

  - stage: docker build and publish
    script:
    - docker build --target production --tag todo-app:prod .
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
    - docker push kiranuppi84/todo-app:prod


notifications:
  email:
    recipients:
      - kiranuppi@gmail.com
    on_success: always
    on_failure: always
  slack: corndeldevops-jul3809:vz4EBKT04B7AidKS3MCzAVBs